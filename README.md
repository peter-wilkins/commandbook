# Commandbook

Commandbook is a product idea for a permissioned, inspectable command language
for human phone tasks.

The core idea:

> Commands declare intent; resolvers discover the route.

Instead of giving an AI assistant broad device access, users define a
machine-readable commandbook. Each command declares its inputs, outputs,
capabilities, trust level, side effects, and dry-run behaviour. An AI can then
plan a safe pipeline from intent to action, while the commandbook remains the
security policy, documentation, test suite, and portability layer.

## Seed

This project was seeded from Peter's ChatGPT conversation titled
`Waterproof Case`, captured locally from the day-to-day Android phone on
2026-06-07.

Raw capture is local-only and ignored:

```text
local/source-captures/waterproof-case-chatgpt.txt
```

Committed starting point:

```text
docs/product-seed.md
```
